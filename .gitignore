# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove if you need to vendor)
vendor/

# Go workspace file
go.work
go.work.sum

# Environment configuration files
# IMPORTANT: Add any local config files that shouldn't be committed.
# We use configs/config.yaml which IS committed, but if you had a
# configs/config.local.yaml for overrides, you'd add it here.
# Example:
# configs/local.*.yaml
# .env

# IDE settings
.vscode/
.idea/

# Compiled binary name if you use `go build`
aegis_finance_core # Or whatever your binary will be named
api # if you build cmd/api directly

# OS-generated files
.DS_Store
Thumbs.db